---
phase: 05-responsive-analyst-dashboard
plan: 09
type: execute
gap_closure: true
wave: 7
depends_on:
  - 05-08
files_modified:
  - frontend/src/features/dashboard/components/detail-pane.tsx
  - frontend/src/features/dashboard/components/detail-pane.test.tsx
  - frontend/e2e/dashboard-responsive.spec.ts
  - docs/ops/dashboard-ui.md
autonomous: true
requirements:
  - SEC-02
must_haves:
  truths:
    - "Detail pane displays masked source IP by default and never exposes raw IP text in public analyst views."
    - "List and detail IP rendering stay consistent by routing both through the shared masking utility."
    - "Component and browser tests fail if detail-pane masked-IP behavior regresses."
  artifacts:
    - path: frontend/src/features/dashboard/components/detail-pane.tsx
      provides: Detail-pane source IP rendering updated to apply default masking.
    - path: frontend/src/features/dashboard/components/detail-pane.test.tsx
      provides: Regression tests that assert masked detail output and raw-IP absence.
    - path: frontend/e2e/dashboard-responsive.spec.ts
      provides: Browser-level proof that selected-prisoner detail shows masked source IP.
    - path: docs/ops/dashboard-ui.md
      provides: Runbook checkpoint explicitly validating masked source IP in detail view.
  key_links:
    - from: frontend/src/features/dashboard/components/detail-pane.tsx
      to: frontend/src/features/dashboard/domain/masking.ts
      via: Detail source IP field uses shared masking helper before render.
    - from: frontend/src/features/dashboard/components/detail-pane.test.tsx
      to: frontend/src/features/dashboard/components/detail-pane.tsx
      via: Tests enforce masked formatting and verify no raw source IP appears.
    - from: frontend/e2e/dashboard-responsive.spec.ts
      to: frontend/src/features/dashboard/components/detail-pane.tsx
      via: E2E selection flow confirms masked detail presentation in the shipped UI.
---

<objective>
Close the Phase 05 `SEC-02` verification gap by enforcing masked source IP display in the detail pane and locking it with regression coverage.

Purpose: Verification found one remaining public-safety leak path (unmasked detail IP); this plan resolves that gap without reopening unrelated dashboard scope.
Output: Masked-by-default detail IP rendering plus component/E2E/runbook proof for sustained compliance.
</objective>

<execution_context>
@/Users/rob/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rob/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-responsive-analyst-dashboard/VERIFICATION.md
@.planning/phases/05-responsive-analyst-dashboard/05-05-PLAN.md
@.planning/phases/05-responsive-analyst-dashboard/05-06-PLAN.md
@frontend/src/features/dashboard/domain/masking.ts
@frontend/src/features/dashboard/components/prisoner-row.tsx
@frontend/src/features/dashboard/components/detail-pane.tsx
@frontend/src/features/dashboard/components/detail-pane.test.tsx
@frontend/e2e/dashboard-responsive.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply default IP masking to detail-pane source IP rendering</name>
  <files>frontend/src/features/dashboard/components/detail-pane.tsx</files>
  <behavior>
    - Detail-pane source IP uses the shared masking utility, matching row-level masking posture.
    - v1 public analyst UI does not introduce unmask toggles or alternate raw-IP paths.
    - Existing detail summary/intel/activity rendering behavior remains unchanged.
  </behavior>
  <action>Update detail-pane source IP rendering to pass prisoner `source_ip` through the existing dashboard masking domain helper before display, preserving all current layout/state behavior while eliminating raw-IP exposure.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run test -- --run src/features/dashboard/components/detail-pane.test.tsx</automated>
  </verify>
  <done>Selected-prisoner detail always renders masked source IP in public dashboard flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression coverage and operator check for masked detail IP</name>
  <files>frontend/src/features/dashboard/components/detail-pane.test.tsx, frontend/e2e/dashboard-responsive.spec.ts, docs/ops/dashboard-ui.md</files>
  <behavior>
    - Component tests assert masked output and absence of raw source IP text in detail pane.
    - Dashboard E2E validates masked detail source IP in a real selection flow.
    - Ops runbook includes an explicit detail-pane masked-IP verification checkpoint.
  </behavior>
  <action>Extend detail-pane unit tests and dashboard responsive E2E assertions to prove masked detail output, then update the operator runbook with a clear check that detail views never expose full source IP values.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run test -- --run src/features/dashboard/components/detail-pane.test.tsx &amp;&amp; npm run test:e2e -- --project=chromium --grep @dashboard</automated>
  </verify>
  <done>Regression safeguards and runbook guidance prevent reintroduction of unmasked detail IP display.</done>
</task>

</tasks>

<verification>
Validate SEC-02 closure end-to-end before marking the gap resolved.
<automated>cd frontend &amp;&amp; npm run test -- --run src/features/dashboard/components/detail-pane.test.tsx &amp;&amp; npm run test:e2e -- --project=chromium --grep @dashboard &amp;&amp; npm run build</automated>
<automated>rg -n "detail|masked|source ip" docs/ops/dashboard-ui.md</automated>
</verification>

<success_criteria>
SEC-02 is closed when the detail pane no longer exposes raw source IP values, automated tests enforce masked rendering, and ops documentation includes explicit detail-view masking checks.
</success_criteria>

<output>
After completion, create `.planning/phases/05-responsive-analyst-dashboard/05-09-SUMMARY.md`.
</output>
